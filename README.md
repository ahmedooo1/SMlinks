# SMlinks

صفحة روابط شخصية (Link Tree) مبنية بـ HTML/CSS/JS خالص.

## النشر

يتم النشر تلقائيًا على GitHub Pages عند كل push إلى الفرع `main` عبر GitHub Actions
(`.github/workflows/deploy.yml`).

بعد أول push، فعّل GitHub Pages من:
`Settings → Pages → Source → GitHub Actions`
